/**
 * Unit Tests: DemoLoaderService Offline Behavior
 *
 * NOTE: Feature 028 removed the onboarding/DemoLoaderService entirely.
 * All tests in this file are skipped — they test deleted code.
 */

import { describe, it } from 'vitest';

describe.skip('DemoLoaderService - Offline Mode (Feature 025, US1) [DELETED in Feature 028]', () => {
  it('placeholder', () => {});
});
